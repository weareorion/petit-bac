/// App-wide non-visual constants.
class AppConstants {
  AppConstants._();

  /// Matches [pubspec.yaml] version field.
  static const appVersion = '1.0.0';

  static const defaultTimerSeconds = 102;
  static const pointsPerValidAnswer = 10;
}
